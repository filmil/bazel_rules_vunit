load("@bazel_rules_nvc//build/nvc:rules.bzl", "vhdl_library")

vhdl_library(
    name = "vunit",
    # do not sort
    srcs = [
        "@vunit//:srcs",
        "@vunit//:data_types_srcs_pkg",
        "@vunit//:logging_src_pkg",
        "@vunit//:data_types_srcs",
        "@vunit//:logging_src_pkg_body",
        "@vunit//:check_srcs",
        "@vunit//:random_srcs", # Needs osvvm library.
        "@vunit//:path_srcs",
    ],
    deps = [
        "@bazel_rules_osvvm//tool.nvc:osvvm", # Needs standard = "2019", ripple effect.
    ],
    standard = "2019",
)
