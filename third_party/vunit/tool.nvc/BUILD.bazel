load("@rules_nvc//build/nvc:rules.bzl", "vhdl_library")

package(default_visibility = ["//visibility:public"])

vhdl_library(
    name = "vunit_lib",
    # do not sort
    srcs = [
        "@vunit//:srcs",
        "@vunit//:random_srcs", # Needs osvvm library.
        "@vunit//:path_srcs",
    ],
    deps = [
        "@rules_osvvm//tool.nvc:osvvm", # Needs standard = "2019", ripple effect.
    ],
    standard = "2019",
)
